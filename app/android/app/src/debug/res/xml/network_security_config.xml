<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only: the Android emulator's host-loopback alias (10.0.2.2) and
  localhost are plain HTTP in local development, but Android blocks
  cleartext traffic by default for apps targeting API 28+. This carves out
  just those two dev hosts rather than disabling cleartext protection
  app-wide. Not included in release — a real deployment should use HTTPS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
